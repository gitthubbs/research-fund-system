\chapter{系统总体设计}
\raggedbottom

\section{系统架构设计}
在构建这套系统时，本研究选取了目前业界比较成熟的 B/S（Browser/Server）架构，并严格按照前后端分离的思路进行模块划分。这样做的目的很明确：一方面是为了保证财务数据在后端逻辑中的安全性，另一方面也方便以后对业务功能进行平滑扩展。

\subsection{技术架构概述}
系统后端是以 Spring Boot 为核心搭建的。开发过程中主要利用了其依赖注入和切面编程特性，将业务逻辑与底层服务有效解耦，从而提升了代码的可维护性。数据持久层则选用了 MyBatis-Plus，配合 Lambda 表达式，既能保证 SQL 的安全性，也简化了持久层开发。前端部分，通过 Vue 3 的组合式 API 和 Element Plus 组件库，系统能够灵活地构建高响应性的交互界面。系统的总体技术架构如图\ref{fig:architecture}所示。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{figures/architecture.png}
    \caption{系统总体技术架构图}
    \label{fig:architecture}
\end{figure}


\subsection{请求流向的逻辑处理}
为了让数据在各层之间的流动更加有序，本系统遵循了严谨的逻辑分层规范。当用户在浏览器端发起一个报销申请时，请求首先会到达表示层，前端在进行基本的输入校验后，会把数据封装成 JSON 报送给后端的 RESTful 接口。

后端的 Controller 层在接收到请求后，会先执行鉴权逻辑，确认无误后再把控制权交给业务逻辑层（Service）。作为系统的“中枢”，Service 层会执行复杂的业务检查，例如在支出记录产生时，程序会调动项目余额并运行多维预警模型。最后，持久层会执行 MyBatis-Plus 生成的 SQL 语句，完成与 MySQL 数据库的交互。这种分层方式虽然增加了一些代码量，但确实显著提升了逻辑的可维护性。以系统核心的智能风险扫描业务为例，其在各层级间的交互时序如图\ref{fig:sequence_risk}所示。

\begin{figure}[H]
    \centering
    \includegraphics[width=1.0\textwidth]{figures/sequence_risk.png}
    \caption{智能风险辅助扫描业务时序图}
    \label{fig:sequence_risk}
\end{figure}


\section{功能模块设计}
根据科研经费管理的业务全貌，系统包含五个核心模块，其功能结构如图\ref{fig:function_modules}所示。

\begin{figure}[H]
    \centering
    \includegraphics[width=1.0\textwidth]{figures/function_modules.png}
    \caption{系统功能模块结构图}
    \label{fig:function_modules}
\end{figure}



\subsection{用户与权限管理}
安全是财务系统的核心基石。本设计采用了 RBAC 模型来管理权限，将用户明确划分为科研人员和管理员两个层级。在数据存储方面，为确保用户信息安全，程序强制对密码进行 BCrypt 哈希加密处理。此外，系统还提供个人资料维护及密码修改功能。

\subsection{科研项目全生命周期管理}
这个模块更像是一个驱动业务的状态机。系统构建了一套包含 6 个状态的流转逻辑，核心在于“节点联动”：比如只有等管理员审核完立项，后续的预算编制功能才会解锁。系统持续监控项目的时间进度，这为后续的多维分析提供了必要的时间参考。

\subsection{预算编制与额度控制}
预算管理的核心其实就是“把钱分清楚”。系统引入了科目体系（Fund Category），支持给每个项目分配合理的预算比例。为了防止录入错误，程序内置了一个强校验算法：各科目预算之和必须正好等于批复总额，只要差一分钱，逻辑上就会自动拦截。

\subsection{支出报销与流程审批}
这是经费流动的实际出口，设计遵循的是“余额硬拦截”原则。只要报销金额超过了该科目的当前余额，系统就会自动阻断申报。同时，报销流程也设计了财务初审和复核，每一笔钱花出去都得经过双人审核并留下审计痕迹。报销申请的详细审批与拦截流程如图\ref{fig:flowchart_reimbursement}所示。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{figures/flowchart_reimbursement.png}
    \caption{报销申请审批与自动拦截流程图}
    \label{fig:flowchart_reimbursement}
\end{figure}


\subsection{多维决策看板}
这是本系统最智能的部分，它依托于数据快照（Snapshot）进行统计。通过 ECharts，原本枯燥的执行率、时间偏差以及科目占比被转化为了直观的图表。该模块的设计核心在于“主动监测”，系统会自动计算时间进度和经费执行率的差值，帮管理员一眼看出那些执行滞后的项目。

\section{数据库设计}

\subsection{数据库概念模型分析}
在数据建模阶段，系统主体围绕科研业务流的核心实体进行设计排布。项目负责人与项目之间是一对多的关系，这符合一名老师负责多个项目的实际。而项目则是整个数据网的中心：一个项目对应多个预算科目分配，同时也产生多笔支出流水。

经费科目作为字典基准，它横向贯穿了预算和支出。通过这种关联设计，系统能很方便地把每一笔报销记录归集到对应的科目下，从而实现多维度的聚合统计。如图\ref{fig:er_full}所示，这种 E-R 关系保证了数据的一致性和查询解析的灵活性。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{figures/er-full.png}
    \caption{系统总体 E-R 图}
    \label{fig:er_full}
\end{figure}
\ctexset{subsection/afterskip=5pt}
\subsection{数据库逻辑结构设计}
根据前述系统架构与功能模块的设计，本研究将业务逻辑映射为关系数据库结构。在数据建模过程中，遵循第三范式要求以减少冗余，同时针对统计看板等高频查询场景保留了部分冗余设计以提升响应效率。以下详细列出系统核心实体的物理表结构。

系统用户表如表\ref{tab:sys_user}所示，主要用于存储各类操作主体的基础信息，包括用于身份验证的加密凭证以及界定权限边界的角色标识。
\begin{table}[H]
    \centering
    \caption{系统用户表（sys\_user）结构}
    \label{tab:sys_user}
    \zihao{-4}\songti
    \begin{tabularx}{\textwidth}{cccY}
    \toprule
    字段名 & 类型 & 主键 & 说明 \\ \midrule
    id & bigint & 是 & 用户内部 ID（自增） \\
    username & varchar(50) & 否 & 登录用户名 \\
    password & varchar(100) & 否 & BCrypt 哈希加密密码 \\
    role & varchar(20) & 否 & 角色（admin/manager/researcher） \\
    real\_name & varchar(50) & 否 & 真实姓名 \\
    department & varchar(100) & 否 & 所属学院/部门 \\
    phone & varchar(20) & 否 & 联系电话 \\
    email & varchar(100) & 否 & 电子邮箱 \\
    create\_time & datetime & 否 & 注册时间 \\ \bottomrule
    \end{tabularx}
\end{table}
\vspace{-1.5em}
科研项目表如表\ref{tab:research_project}所示，是整套业务逻辑的锚点，它详细记录了项目的基本属性、时间跨度、经费总量，并实时维护项目的流转状态。
\begin{table}[H]
    \centering
    \caption{科研项目表（research\_project）结构}
    \label{tab:research_project}
    \zihao{-4}\songti
    \begin{tabularx}{\textwidth}{cccY}
    \toprule
    字段名 & 类型 & 主键 & 说明 \\ \midrule
    id & bigint & 是 & 项目 ID \\
    project\_name & varchar(100) & 否 & 项目全称 \\
    project\_code & varchar(50) & 否 & 财务项目编号 \\
    principal\_id & bigint & 否 & 负责人 ID \\
    start\_date & date & 否 & 开始时间 \\
    end\_date & date & 否 & 结束时间 \\
    total\_budget & decimal(12,2) & 否 & 项目总预算 \\
    status & tinyint & 否 & 状态（0-草稿/1-待审/2-立项/3-驳回等） \\
    audit\_remark & varchar(255) & 否 & 审批意见/驳回原因 \\
    update\_time & datetime & 否 & 数据最后更新时间 \\ \bottomrule
    \end{tabularx}
\end{table}
\vspace{-1.5em}
经费科目表如表\ref{tab:fund_category}所示，定义了系统内部公用的经费分类字典，它是预算编制和支出核算的基础参照标准。
\begin{table}[H]
    \centering
    \caption{经费科目表（fund\_category）结构}
    \label{tab:fund_category}
    \zihao{-4}\songti
    \begin{tabularx}{\textwidth}{cccY}
    \toprule
    字段名 & 类型 & 主键 & 说明 \\ \midrule
    id & bigint & 是 & 科目 ID \\
    category\_name & varchar(50) & 否 & 科目名称 \\ \bottomrule
    \end{tabularx}
\end{table}
\vspace{-1.5em}
经费预算表如表\ref{tab:fund_budget}所示，通过建立项目与科目的多对多映射关系，具体落实了每个科研项目在不同维度下的核定资金额度。
\begin{table}[H]
    \centering
    \caption{经费预算表（fund\_budget）结构}
    \label{tab:fund_budget}
    \zihao{-4}\songti
    \begin{tabularx}{\textwidth}{cccY}
    \toprule
    字段名 & 类型 & 主键 & 说明 \\ \midrule
    id & bigint & 是 & 预算项 ID \\
    project\_id & bigint & 否 & 项目 ID \\
    category\_id & bigint & 否 & 经费类别 ID \\
    budget\_amount & decimal(12,2) & 否 & 分项核定预算金额 \\ \bottomrule
    \end{tabularx}
\end{table}


经费支出记录表如表\ref{tab:fund_expenditure}所示，详细捕捉了每一笔实际发生的报销业务，它是系统进行实时余额阻断校验与动态执行率分析的数据来源。
\begin{table}[htbp]
    \centering
    \caption{经费支出记录表（fund\_expenditure）结构}
    \label{tab:fund_expenditure}
    \zihao{-4}\songti
    \begin{tabularx}{\textwidth}{cccY}
    \toprule
    字段名 & 类型 & 主键 & 说明 \\ \midrule
    id & bigint & 是 & 记录 ID \\
    project\_id & bigint & 否 & 项目 ID \\
    category\_id & bigint & 否 & 经费类别 ID \\
    amount & decimal(12,2) & 否 & 报销金额 \\
    expenditure\_date & date & 否 & 支出实际日期 \\
    status & tinyint & 否 & 状态（1-待审/2-通过/3-驳回） \\
    applicant\_id & bigint & 否 & 申请报销人 ID \\
    is\_read & tinyint(1) & 否 & 消息是否已读 \\ \bottomrule
    \end{tabularx}
\end{table}


为了应对科研过程中不可预见的变更，预算调剂表（如表\ref{tab:fund_adjustment}所示）记录了科目间额度的划转过程及相应的审批决策支持信息。
\begin{table}[htbp]
    \centering
    \caption{预算调剂表（fund\_adjustment）结构}
    \label{tab:fund_adjustment}
    \zihao{-4}\songti
    \begin{tabularx}{\textwidth}{cccY}
    \toprule
    字段名 & 类型 & 主键 & 说明 \\ \midrule
    id & bigint & 是 & 调剂记录 ID \\
    project\_id & bigint & 否 & 关联项目 ID \\
    from\_category\_id & bigint & 否 & 调出科目 ID \\
    to\_category\_id & bigint & 否 & 调入科目 ID \\
    amount & decimal(19,4) & 否 & 调剂金额（高精度） \\
    status & int & 否 & 状态（0-待审/1-通过/2-驳回） \\
    applicant\_id & bigint & 否 & 调剂发起人 ID \\ \bottomrule
    \end{tabularx}
\end{table}


经费统计快照表如表\ref{tab:fund_statistics_snapshot}所示，是针对多维决策看板进行的性能优化设计，它异步存储了项目的执行率、占比及趋势等计算密集型指标。
\begin{table}[htbp]
    \centering
    \caption{经费统计快照表（fund\_statistics\_snapshot）结构}
    \label{tab:fund_statistics_snapshot}
    \zihao{-4}\songti
    \begin{tabularx}{\textwidth}{cccY}
    \toprule
    字段名 & 类型 & 主键 & 说明 \\ \midrule
    id & bigint & 是 & 快照 ID \\
    project\_id & bigint & 否 & 项目 ID \\
    execution\_rate & decimal(5,2) & 否 & 实时经费执行率 \\
    category\_data & text & 否 & 分类占比 JSON 数据 \\
    trend\_data & text & 否 & 趋势分析 JSON 数据 \\ \bottomrule
    \end{tabularx}
\end{table}


系统日志表如表\ref{tab:sys_log}所示，负责记录所有关键业务操作的审计线索，确保了财务管理过程中的每一步敏感操作均具备可溯源性。
\begin{table}[htbp]
    \centering
    \caption{系统日志表（sys\_log）结构}
    \label{tab:sys_log}
    \zihao{-4}\songti
    \begin{tabular}{cccc}
    \toprule
    字段名 & 类型 & 主键 & 说明 \\ \midrule
    id & bigint & 是 & 日志 ID \\
    user\_id & bigint & 否 & 操作用户 ID \\
    action & varchar(200) & 否 & 业务操作行为 \\
    ip\_address & varchar(45) & 否 & 访问 IP 地址 \\
    create\_time & datetime & 否 & 操作发生时间 \\ \bottomrule
    \end{tabular}
\end{table}

\section{本章小结}
本章论述系统总体方案。架构选型定位于前后端分离。底层业务响应机制实施多维逻辑分层，核心目标在于维持流转有序。项目、预算、支出等管控策略被拆解为五个功能模块。数据库建模涵盖从 E-R 关系映射到物理表结构的完整路径。但在设计中发现，多维分层导致的模块间调用链路比预期更长，这种逻辑深度在一定程度上增加了联调阶段的复杂度。
