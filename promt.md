你是一个经验丰富的全栈开发工程师，正在对一个**已完成部分功能的高校科研经费管理系统**进行增量开发。请严格遵循以下要求执行任务。

---

# 一、项目背景（必须理解）

该系统基于：

* 后端：Spring Boot（RESTful API）
* 前端：Vue3 + Element Plus + ECharts
* 数据库：MySQL（已设计完成）

系统已具备：

* 基础项目结构（前后端分离）
* 数据库表（project / budget / expenditure 等已存在）
* 基础页面与接口（部分已实现）

你的任务不是从零开发，而是：

👉 **在现有系统基础上补全“科研经费管理完整业务链路”**

---

# 二、开发原则（必须严格遵守）

1. **禁止破坏已有代码**

    * 不得删除现有字段、接口、类
    * 不得重命名已有接口
    * 只能“新增或扩展”

2. **所有新增或修改代码必须标注**

   ```java
   // ★ 新增
   // ★ 修改
   ```

3. **保持现有项目结构**

    * 后端遵循 controller / service / mapper 分层
    * 前端遵循 views / api / components 结构

4. **优先复用已有代码**

    * 如果已有类似接口 → 扩展
    * 不重复造轮子

---

# 三、本次需要实现的功能（核心任务）

目标：打通完整业务链路：

```text
项目 → 预算 → 支出 → 统计分析
```

---

## 1️⃣ 项目模块增强（Project）

### 任务：

* 检查是否已有项目CRUD
* 若已有 → 补充以下能力：

```yaml
新增能力:
  - 查询项目时返回：
    - 总预算
    - 已使用金额（自动统计）
    - 剩余金额
    - 执行率
```

### 后端要求：

* 在 Service 层计算：

```java
已用金额 = sum(expenditure.amount)
执行率 = 已用 / 总预算
```

---

## 2️⃣ 预算模块补全（Budget）

### 任务：

```yaml
实现：
  - 按项目设置预算（分类维度）
  - 查询项目预算列表
```

### 校验逻辑（必须实现）：

```yaml
规则：
  - 同一项目 + 分类 不允许重复
```

---

## 3️⃣ 支出模块增强（Expenditure）

### 任务：

```yaml
实现：
  - 新增支出
  - 条件查询（项目 + 分类 + 时间范围）
```

### 必须新增逻辑：

```yaml
自动计算：
  - 项目已使用金额（用于统计）
```

### 可选增强（加分项）：

```yaml
校验：
  - 支出金额不能超过该分类预算
```

---

## 4️⃣ 统计分析模块（重点新增）⭐

必须新增以下接口：

---

### （1）项目执行率

```http
GET /api/statistics/project-execution
```

返回：

```json
[
  {
    "projectName": "...",
    "executionRate": 0.6
  }
]
```

---

### （2）分类支出占比

```http
GET /api/statistics/category
```

---

### （3）月度支出趋势

```http
GET /api/statistics/monthly
```

---

## 5️⃣ 前端可视化（ECharts）

### 在已有页面基础上扩展：

```yaml
新增图表：
  - 柱状图：项目执行率
  - 饼图：分类占比
  - 折线图：月度趋势
```

### 要求：

* 使用 ECharts
* 封装为组件（如 ChartCard.vue）
* 从后端接口获取数据

---

# 四、数据库使用要求

* 不修改已有表结构
* 允许：

  ```sql
  -- ★ 新增字段（如果确实必要）
  ```
* 优先通过 SQL 聚合实现统计

---

# 五、输出要求（非常重要）

请按以下结构输出代码：

---

## 1️⃣ 后端

```yaml
- Controller
- Service
- Mapper
- SQL（如有）
```

---

## 2️⃣ 前端

```yaml
- API封装（api/*.js）
- 页面（views）
- 图表组件（components）
```

---

## 3️⃣ 修改说明

```yaml
列出：
  - 修改了哪些文件
  - 新增了哪些功能
```

---

# 六、执行策略（Agent必须遵守）

按顺序执行：

```yaml
step1: 分析现有代码结构（假设已有基础）
step2: 补全项目统计字段
step3: 完善预算与支出逻辑
step4: 实现统计接口
step5: 接入前端图表
```

---

# 七、禁止行为

```yaml
禁止：
  - 重写整个系统
  - 修改数据库核心结构
  - 删除已有代码
  - 使用复杂大数据框架（如Spark）
```

---

# 八、最终目标

系统应达到：

```yaml
科研人员可以：
  - 查看项目经费使用情况
  - 录入支出
  - 查看图表

管理人员可以：
  - 管理项目与预算
  - 查看所有统计分析
```

---

请直接开始生成代码，不需要解释过程。
